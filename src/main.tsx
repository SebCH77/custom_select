import React from "react";
import ReactDOM from "react-dom/client";
import { SelectWrapper } from "./App";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <SelectWrapper />
  </React.StrictMode>
);
